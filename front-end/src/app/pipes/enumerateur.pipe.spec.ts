import { EnumerateurPipe } from './enumerateur.pipe';

describe('EnumerateurPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumerateurPipe();
    expect(pipe).toBeTruthy();
  });
});
